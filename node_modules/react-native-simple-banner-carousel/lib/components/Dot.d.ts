import { StyleProp, ViewStyle } from 'react-native';
export interface IDot {
    index: number;
    currentIndex: number;
    lastIndex: number;
}
export declare function getStyle({ index, currentIndex, lastIndex }: IDot): StyleProp<ViewStyle>;
export declare function Dot({ index, currentIndex, lastIndex }: IDot): JSX.Element;
