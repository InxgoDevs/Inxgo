export interface IBanner {
    id: string;
    source: any;
    width: number;
    onPress?: (id: string) => void;
}
export declare function Banner({ id, width, source, onPress }: IBanner): JSX.Element;
