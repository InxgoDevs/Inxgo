export interface IIndicator<T> {
    data: T[];
    currentIndex: number;
    lastIndex: number;
}
export declare function Indicator<T>({ data, currentIndex, lastIndex }: IIndicator<T>): JSX.Element;
