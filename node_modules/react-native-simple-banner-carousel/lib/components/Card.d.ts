import { StyleProp, ViewStyle } from 'react-native';
export interface ICard {
    id: string;
    title: string;
    width: number;
    source: any;
    description: string;
    score: number;
    onPress: () => void;
    style?: StyleProp<ViewStyle>;
}
export declare function Card({ id, source, width, title, description, score, style }: ICard): JSX.Element;
